[package]
authors     = ["magic-akari <akari.ccino@gmail.com>"]
description = "[SWC plugin] workaround for jest"
edition     = "2021"
homepage    = "https://github.com/magic-akari/jest_workaround"
keywords    = ["swc-plugin", "jest"]
license     = "MIT"
name        = "jest_workaround"
readme      = "README.md"
repository  = "https://github.com/magic-akari/jest_workaround"
version     = "0.1.10"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
indexmap = "1.9.1"
serde = "1.0.144"
swc_core = { version = "0.20.2", features = [
    "ecma_ast",
    "ecma_visit",
    "swc_plugin",
    "ecma_utils",
    "swc_atoms",
    "plugin_transform",
] }


[dev-dependencies]
swc_core = { version = "0.20.2", features = ["testing_transform"] }
testing  = "0.29.4"

[profile.release]
strip = "debuginfo"
